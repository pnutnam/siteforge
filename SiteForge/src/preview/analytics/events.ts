/**
 * Analytics events table and recording.
 * This file re-exports the analyticsEvents table from schema for convenience.
 */

import { analyticsEvents } from '../../database/schema';

export { analyticsEvents };
